import { type PortfolioItem } from "./types";

// Mobile app portfolio entries.
// Add mobile projects here — they'll be merged and sorted automatically.
export const mobilePortfolios: Omit<PortfolioItem, "id">[] = [
  // entry("Example App", "mobile", C.SERVICES, appCover, "https://...", "...", ["React Native", "Expo"]),
];
